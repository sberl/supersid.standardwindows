
/**
 * assets
 * 
 * Provided by AIR application.
 * The array contains objects with a reference to the cItem 'id' and 'url' for the asset to load.
 */
var assets = new Array();
assets.push({id: "library_asset_mars", url: "../text/library_asset_mars.txt"});
assets.push({id: "library_asset_mars_image01", url: "../text/library_asset_mars_image01.jpg"});

if(assets.length > 0){
	
	for (var asset_index = 0, len = assets.length; asset_index < len; asset_index++) {
		
		
		
	}
	
}